{
  "name": "vaml-translator-backend",
  "version": "0.2.0",
  "private": true,
  "description": "Authenticated Vercel API boundary for VAML translation and agent chat.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check api/health.js && node --check api/v1/translate.js && node --check api/v1/chat.js"
  }
}
